{
  "name": "project-root",
  "private": true,
  "workspaces": ["backend", "frontend/nextjs"],
  "scripts": {
    "install-all": "npm install --workspaces",
    "start-backend": "npm run dev --workspace=backend",
    "start-frontend": "npm run dev --workspace=frontend/nextjs",
    "start-all": "npm run start-backend & npm run start-frontend"
  }
}

